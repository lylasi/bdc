# 雙語基礎與 AI 提示詞集中遷移驗證報告

## 1. 驗證範圍

本次驗證涵蓋：

- 大陸簡體 `zh-CN`／香港繁體 `zh-HK` 語言設定與舊設定兼容。
- 主導航、文件標題及設定入口的 DOM 翻譯。
- 全域設定中的語言切換與 localStorage 持久化。
- `modules/prompts/` 集中提示詞 registry。
- Dictionary、Learning、Article、QA、OCR、Dictation、Assistant、Import 提示詞。
- Article 及 Dictionary AI 快取的語言與提示詞版本隔離。
- 靜態 HTTP 環境下的新模組載入。

## 2. 已執行驗證

### 2.1 JavaScript 語法

- 對 `main.js`、`ai-config.example.js`、`modules/`、`features/` 及 `locales/` 下所有 JavaScript 執行 `node --check`。
- 結果：全部通過。

### 2.2 設定兼容

- 無既有設定時，`language.interfaceLocale` 預設為 `zh-CN`。
- 舊版 `pen_global_settings` 沒有 language 欄位時，可自動合併預設語言。
- 舊版 assistant 等既有欄位不會在合併時遺失。
- 結果：全部通過。

### 2.3 DOM 語言切換

使用模擬 DOM 驗證：

- `<html lang>` 可切換為 `zh-CN`／`zh-HK`。
- `data-i18n` 文字可更新。
- `title`、`aria-label`、`placeholder` 可更新。
- locale change 事件包含正確 locale。
- 語言設定可寫入 localStorage。
- 非法 locale 可回退 `zh-CN`。
- 結果：全部通過。

### 2.4 翻譯鍵一致性

- `zh-CN` 與 `zh-HK` 各有 33 個翻譯鍵。
- 兩套翻譯鍵集合完全一致。
- 目前程式中引用的 33 個翻譯鍵全部存在。
- 結果：全部通過。

### 2.5 提示詞 registry

- 共驗證 14 個 Prompt ID，分布於 8 個功能檔案。
- 每個提示詞均有「對應功能、呼叫位置、輸出格式、語言策略」備註。
- 所有模板變數均可正確替換，沒有殘留 `${variable}`。
- 所有 `follow-interface` 提示詞可分別注入大陸簡體及香港繁體規則。
- OCR 純文字擷取及文章清理等 `preserve-source` 任務不會被注入中文輸出規則。
- QA 可正確產生 system/user messages。
- 結果：全部通過。

### 2.6 提示詞殘留掃描

- `modules/` 與 `features/` 已不存在 `AI_PROMPTS` 讀取。
- Assistant 已不存在本地 `SYSTEM_PROMPT`。
- `ai-config.example.js` 已移除舊提示詞副本，並指向 `modules/prompts/`。
- 私有 `ai-config.js` 未修改；其中舊提示詞設定目前不再被產品程式讀取。
- 結果：通過。

### 2.7 快取隔離

- Dictionary 單字分析快取包含 locale、prompt version 及模型設定。
- Article 段落、單字、句子及片語快取 schema 升級為 `v2`。
- Article 快取包含 locale 及 prompt version。
- 同一輸入的 `zh-CN` 與 `zh-HK` 快取 key 不同。
- 提示詞版本變更後快取 key 不同。
- 結果：全部通過。

### 2.8 Learning 固定前綴兼容

- 發現原介面只識別「正確／不正確」，簡體 AI 回傳「正确／不正确」時會誤判。
- 已修改提示詞，要求模型輸出 locale 對應的固定前綴。
- 已修改 Learning 解析器，同時識別簡體及繁體前綴。
- 對應結果提示已加入 i18n。
- 結果：問題已修復，語法及模板驗證通過。

### 2.9 Dictation 結果兼容

- 原 Markdown 報告渲染器只識別「正確／錯誤」。
- 已同時支援「正确／错误」。
- 結果：語法驗證通過。

### 2.10 模組載入及 HTTP

- 以本機 HTTP server 提供靜態檔案。
- 驗證 `index.html`、`main.js`、i18n、locales 及所有 prompts 模組均返回 HTTP 200 且內容非空。
- 以 Node ES Module 動態載入 `modules/api.js`、`modules/cache.js`、`modules/settings.js`。
- 結果：全部通過。

### 2.11 Diff 品質

- `git diff --check` 通過。
- 沒有修改私有 `ai-config.js`。
- `AGENTS.md` 的既有修改不是本次實作產生。

## 3. 本次繼續完成的實作

- 完成 Article 段落、逐句、句子結構、單字 tooltip 及片語提示詞遷移。
- 完成 QA 判題提示詞及語言化 cache key。
- 完成 OCR 原文擷取與手寫 JSON 批改提示詞遷移。
- 完成 Dictation Markdown 批改提示詞遷移。
- 完成 Article Assistant system prompt 遷移。
- 完成 Markdown 清理及 HTML 正文擷取提示詞遷移。
- 完成所有上述 AI 任務的 `zh-CN`／`zh-HK` 語言接入。

## 4. 尚未完成的範圍

目前 AI 提示詞層已完成集中遷移，但完整介面雙語仍需分模組進行：

- Vocabulary 大部分動態文案。
- Dictation、Quiz 大部分介面文案。
- Article、OCR 大部分介面文案。
- QA、Sync、Assistant 大部分動態介面文案。
- `index.html` 主內容區的大量靜態文案。

目前只翻譯了主導航、文件標題、設定語言區及 Learning 造句檢查相關文案，因此簡體模式仍可能看到尚未遷移的繁體 UI。

## 5. 仍需人工驗證

專案沒有瀏覽器自動化測試框架，且目前環境沒有 Chromium、Playwright 或 Puppeteer。以下項目仍需在真實瀏覽器手動巡檢：

1. 開啟全域設定，切換兩種語言並重新整理。
2. 實際使用真實 AI API 測試 14 個提示詞任務的輸出用詞。
3. 驗證香港繁體不使用臺灣詞彙或粵語口語。
4. 驗證簡體模式的 Learning 与 Dictation 正確／錯誤狀態樣式。
5. 驗證 Article 同一內容在兩種語言下不共用快取。
6. 驗證 OCR 原文不會因介面語言切換而轉換簡繁。
7. 開啟 `test-floating-statusbar.html`，確認設定持久化改動沒有影響浮動控制。

## 6. 驗證結論

自動化及靜態驗證未發現阻斷問題。第一批雙語基礎與第二批 AI 提示詞集中遷移可繼續保留。下一階段應按模組遷移完整 UI 文案，優先處理共用 UI、Vocabulary、Learning、Dictation 及 Quiz，再處理 Article、OCR、QA、Sync 與 Assistant。
